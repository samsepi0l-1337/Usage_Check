[package]
name = "usage-core"
version = "0.1.4"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }

[features]
default = ["edition-free"]
edition-free = []
edition-pro = []
